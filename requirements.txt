# 基础环境依赖
python-dotenv==1.0.0              # 环境变量管理
python-telegram-bot==20.3         # Telegram Bot API 框架
nest-asyncio==1.5.6               # 解决异步事件循环冲突

# 新增火山引擎 SDK（需安装 ark 扩展功能）
volcengine-python-sdk[ark]==1.3.0  # 火山引擎 Python SDK（ARK 扩展）

# 新增 Groq 接口依赖
groq==0.3.2                       # Groq API 客户端库